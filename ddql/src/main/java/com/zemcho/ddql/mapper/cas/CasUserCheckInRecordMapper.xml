<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zemcho.ddql.mapper.cas.CasUserCheckInRecordMapper">
    <sql id="field">
        user_id,
        nickname,
        phone,
        avatar,
        place_id,
        place_name,
        place_address,
        location,
        check_in_method,
        equipment_id,
        date,
        start_time,
        end_time,
        check_in_time,
        status,
        create_time
    </sql>

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO `cas_user_check_in_record`(
        <include refid="field"></include>
        )VALUES (
        #{data.userId},
        #{data.nickname},
        #{data.phone},
        #{data.avatar},
        #{data.placeId},
        #{data.placeName},
        #{data.placeAddress},
        #{data.location},
        #{data.checkInMethod},
        #{data.equipmentId},
        #{data.date},
        #{data.startTime},
        #{data.endTime},
        #{data.checkInTime},
        #{data.status},
        #{data.createTime}
        )
    </insert>

    <update id="update">
        UPDATE `cas_user_check_in_record`
        SET
        <trim suffixOverrides=",">
            <if test="data.userId != null">user_id = #{data.userId},</if>
            <if test="data.nickname != null">nickname = #{data.nickname},</if>
            <if test="data.phone != null">phone = #{data.phone},</if>
            <if test="data.avatar != null">avatar = #{data.avatar},</if>
            <if test="data.placeId != null">place_id = #{data.placeId},</if>
            <if test="data.placeName != null">place_name = #{data.placeName},</if>
            <if test="data.placeAddress != null">place_address = #{data.placeAddress},</if>
            <if test="data.location != null">location = #{data.location},</if>
            <if test="data.checkInMethod != null">check_in_method = #{data.checkInMethod},</if>
            <if test="data.equipmentId != null">equipment_id = #{data.equipmentId},</if>
            <if test="data.date != null">date = #{data.date},</if>
            <if test="data.startTime != null">start_time = #{data.startTime},</if>
            <if test="data.endTime != null">end_time = #{data.endTime},</if>
            <if test="data.checkInTime != null">check_in_time = #{data.checkInTime},</if>
            <if test="data.status != null">status = #{data.status},</if>
            <if test="data.healthCoin != null">health_coin = #{data.healthCoin},</if>
            <if test="data.goldCoin != null">gold_coin = #{data.goldCoin},</if>
        </trim>
        WHERE id = #{data.id}
    </update>

    <select id="selectByUserIdAndStatus" resultType="com.zemcho.ddql.entity.cas.CasUserCheckInRecord">
        SELECT *
        FROM `cas_user_check_in_record`
        WHERE `user_id` = #{userId}
        AND `status` = #{status}
        ORDER BY `id` DESC
        LIMIT 1
    </select>

    <update id="invalidCheckIn">
        UPDATE `cas_user_check_in_record`
        SET
        `status` = 3
        WHERE `date` = #{date}
        AND `status` = 1
    </update>

    <select id="selectById" resultType="com.zemcho.ddql.entity.cas.CasUserCheckInRecord">
        SELECT *
        FROM `cas_user_check_in_record`
        WHERE `id` = #{id}
    </select>

    <select id="selectList" resultType="com.zemcho.ddql.controller.wechat.index.vo.UserCheckInVo">
        SELECT *
        FROM `cas_user_check_in_record`
        <where>
            <if test="param.searchIntList != null and param.searchIntList.size() > 0">
                AND `id` IN (
                SELECT `record_id` FROM `cas_user_check_in_team_record` WHERE `team_id` IN
                <foreach collection="param.searchIntList" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
                )
            </if>
            <if test="param.searchField1 != null">AND user_id = #{param.searchField1}</if>
            <if test="param.searchField2 != null">AND check_in_method = #{param.searchField2}</if>
            <if test="param.searchField3 != null">AND place_id = #{param.searchField3}</if>
            <if test="param.searchIntStatus != null">AND `status` = #{param.searchIntStatus}</if>
            <if test="param.searchStatusList != null and param.searchStatusList.size() > 0">
                AND `status` IN
                <foreach collection="param.searchStatusList" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="param.startTime != null and param.endTime != null">
                AND `create_time` BETWEEN #{param.startTime} AND #{param.endTime}
            </if>
        </where>
        ORDER BY `id` desc
    </select>

    <select id="selectCount" resultType="com.zemcho.ddql.controller.wechat.index.vo.UserCheckInCountVo">
        SELECT count(1) as total_num, sum(health_coin) as total_health_coin, sum(gold_coin) as total_gold_coin
        FROM `cas_user_check_in_record`
        <where>
            <if test="param.searchIntList != null and param.searchIntList.size() > 0">
                AND `id` IN (
                SELECT `record_id` FROM `cas_user_check_in_team_record` WHERE `team_id` IN
                <foreach collection="param.searchIntList" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
                )
            </if>
            <if test="param.searchField1 != null">AND user_id = #{param.searchField1}</if>
            <if test="param.searchField2 != null">AND check_in_method = #{param.searchField2}</if>
            <if test="param.searchIntStatus != null">AND `status` = #{param.searchIntStatus}</if>
            <if test="param.searchStatusList != null and param.searchStatusList.size() > 0">
                AND `status` IN
                <foreach collection="param.searchStatusList" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="param.startTime != null and param.endTime != null">
                AND `create_time` BETWEEN #{param.startTime} AND #{param.endTime}
            </if>
        </where>
    </select>

    <select id="selectCountByPlace" resultType="com.zemcho.ddql.controller.statistic.vo.PlaceCheckInCountVo">
        SELECT place_id, count(1) as check_in_num
        FROM `cas_user_check_in_record`
        <where>
            <if test="param.searchIntList != null and param.searchIntList.size() > 0">
                AND `id` IN (
                SELECT `record_id` FROM `cas_user_check_in_team_record` WHERE `team_id` IN
                <foreach collection="param.searchIntList" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
                )
            </if>
            <if test="param.searchField1 != null">AND user_id = #{param.searchField1}</if>
            <if test="param.searchField2 != null">AND check_in_method = #{param.searchField2}</if>
            <if test="param.searchIntStatus != null">AND `status` = #{param.searchIntStatus}</if>
            <if test="param.searchStatusList != null and param.searchStatusList.size() > 0">
                AND `status` IN
                <foreach collection="param.searchStatusList" item="item" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            <if test="param.startTime != null and param.endTime != null">
                AND `create_time` BETWEEN #{param.startTime} AND #{param.endTime}
            </if>
        </where>
        group by place_id
    </select>
</mapper>