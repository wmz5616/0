<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zemcho.ddql.mapper.personalCenter.MessageAnnouncementMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into message_announcement(user_id, title, content, is_read, create_time)
        values (#{data.userId}, #{data.title}, #{data.content}, #{data.isRead}, #{data.createTime})
    </insert>
    <update id="update">
        update message_announcement
        <set>
            <if test="data.title != null">
                title = #{data.title},
            </if>
            <if test="data.content != null">
                content = #{data.content},
            </if>
            <if test="data.isRead != null">
                is_read = #{data.isRead},
            </if>
        </set>
        where id = #{data.id}
    </update>

    <update id="read">
        update message_announcement
        set is_read = 1
        where id = #{id}
    </update>

    <update id="readAll">
        update message_announcement
        set is_read = 1
        where user_id = #{userId}
    </update>


</mapper>