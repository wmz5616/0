<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zemcho.ddql.mapper.team.TeamVerificationMapper">


    <insert id="insert">
        insert into team_verification(team_id, license_type, license_image, addition_picture, verification_type, type,
                                      contact_phone, contact_email, status, create_time, update_time, delete_time)
        values (#{data.teamId}, #{data.licenseType}, #{data.licenseImage}, #{data.additionPicture},
                #{data.verificationType}, #{data.type}, #{data.contactPhone}, #{data.contactEmail}, #{data.status},
                #{data.createTime}, #{data.updateTime}, #{data.deleteTime})
    </insert>
    <update id="update">
        update team_verification
        <set>
            <if test="data.licenseType != null">
                license_type = #{data.licenseType},
            </if>
            <if test="data.licenseImage != null">
                license_image = #{data.licenseImage},
            </if>
            <if test="data.additionPicture != null">
                addition_picture = #{data.additionPicture},
            </if>
            <if test="data.verificationType != null">
                verification_type = #{data.verificationType},
            </if>
            <if test="data.type != null">
                type = #{data.type},
            </if>
            <if test="data.contactPhone != null">
                contact_phone = #{data.contactPhone},
            </if>
            <if test="data.contactEmail != null">
                contact_email = #{data.contactEmail},
            </if>
            <if test="data.status != null">
                status = #{data.status},
            </if>
        </set>
        where team_id = #{data.teamId}
    </update>

    <select id="selectList" resultType="com.zemcho.ddql.entity.team.TeamVerification">
        select * from team_verification
        <where>
            <if test="param.searchId != null">
                and team_id = #{param.searchId}
            </if>
            <if test="param.searchIntStatus != null">
                and status = #{param.searchIntStatus}
            </if>
            <if test="param.startTime != null">
                and create_time >= #{param.startTime}
            </if>
            <if test="param.endTime != null">
                and #{param.endTime} >= create_time
            </if>
        </where>
    </select>

    <select id="selectByTeamId" resultType="com.zemcho.ddql.entity.team.TeamVerification">
        SELECT *
        FROM `team_verification`
        WHERE `team_id` = #{teamId}
        ORDER BY `id` DESC
        LIMIT 1
    </select>
</mapper>